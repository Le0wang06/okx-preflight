export default function Home() {
  return (
    <main className="container">
      <h1>OKX Preflight Foundation</h1>
      <p>
        Local development is ready. Next, we will add wallet connection, quote pipelines, and
        preflight risk checks.
      </p>
    </main>
  );
}
