# Server module placeholder

This directory is reserved for server-only services such as:

- OKX API client wrappers
- preflight scoring services
- policy and risk checks
- shared backend error types
